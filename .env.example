NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
NEXT_PUBLIC_APP_NAME=Flus24 Manager

# Comma-separated hostnames allowed to reach the dev server (see next.config.ts).
# Only needed if you access the dev server from a hostname other than localhost.
DEV_ALLOWED_ORIGINS=http://localhost:3000
